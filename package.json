{
  "name": "sagyungdi-workspace",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix uxui-project",
    "build": "npm run build --prefix uxui-project",
    "preview": "npm run preview --prefix uxui-project"
  }
}
